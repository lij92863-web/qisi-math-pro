# Testing Documents

Current executable truth is under `tests/` and `scripts/`. R2 strategy and
counterfactual reports are **active** when named by the closure state/final
report. Historical BM/BMR/P-series reports in this directory are
**audit-only**: retain them for traceability, but do not infer a current pass
without rerunning the referenced gate.
