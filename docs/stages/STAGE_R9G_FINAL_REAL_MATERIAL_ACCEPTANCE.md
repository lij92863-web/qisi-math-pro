# Stage R9G — 最终真实材料与全链路验收

日期：2026-07-22

## 验收边界

- 测试集合固定为 `C:\Users\Administrator\Desktop\题目与答案` 当前全部 16 个文件：13 个 DOCX、3 个 PDF。
- DOCX 走本地确定性链，禁止 AI/OCR；PDF 最终验收仅使用用户授权的第 5 次完整双 PDF 额度。
- 答案、解析、题图和解析图必须有题号与布局证据；不连续、重复或冲突时必须 fail-closed，禁止为追求“满覆盖”而错挂。
- 所有浏览器流程使用隔离数据库；正式题库写入只在一次性上下文验证，用户现有 IndexedDB 不作为破坏性测试场。
- 内容、渲染、图片角色、持久化、入库、检索、选题、组卷、打印和控制台必须同时通过。

## 全文件结构审计

`artifacts/acceptance/r9g-real-material-source-audit.json` 记录了 16 个文件的 SHA-256、包结构、题号骨架、表格、图形、公式对象和 PDF 页数。

- 文件：16/16 可读，失败 0。
- DOCX：13/13 可解包，全部题目文件题号连续；累计识别到 209 个题号骨架。
- PDF：3/3 元数据可读；完整版题目 2 页、完整版答案 4 页、简略版题目 1 页。
- 审计代码无网络、AI 或 OCR 调用。

## DOCX 正常 UI 验收

| 场景 | 结果 | 公式/图片 |
| --- | --- | --- |
| `题目.docx` + `答案.docx` | 14 题、14 答案、14 解析 | 330 个 KaTeX，题 13 有图 |
| `2026年7月9日高中数学作业.docx` | 14/14/14 | 330 个 KaTeX，题 13 有图 |
| `周二晚测.docx` | 12 题，题号连续 | 92 个 KaTeX，题 5/9/11 有图 |
| `高二.docx` | 56 题，54 个安全答案 | 222 个 KaTeX，题 19 有图 |
| 五份广东/河北/湖北大卷 | 每份 19/19，共 95 题 | 774 个 KaTeX；表格、图片行、1/2/4 列选项均保留 |
| 简略双 DOCX | 6/6/6 | 97 个 KaTeX；题图/解析图题号 2/4/6 正确 |
| 完整双 DOCX | 12/12/12 | 231 个 KaTeX；题图 4/8/11，解析图 2/6/8 |

全部 DOCX 浏览器请求 AI/OCR 次数为 0；渲染错误、原始 LaTeX 泄漏、占位符、重复题图和错挂候选均为 0。刷新后的内容、图片 ID 和公式渲染保持一致。完整双 DOCX 的入库、题库检索、选题、组卷和 A4 打印通过，坏图、溢出、原始标记均为 0。

### `高二.docx` 的 48、49 题

源文档答案区没有第 48 题的明确答案标记，并为第 49 题提供了两个互相冲突的标记。系统保留 48、49 未解析，同时继续按显式题号正确绑定第 50—56 题。这是通用的重复/缺失标记 fail-closed 规则；自动补齐或顺移会制造错答案，因此不得把 54/56 改写成虚假的 56/56。

## 第 5 次完整双 PDF 最终验收

- 调用：7 次，未超过 8 次硬上限。
- 模型：`qwen-vl-plus` 2 次、`qwen-vl-ocr-latest` 4 次、`qwen-plus` 1 次；未调用精准/高价升级模型。
- 题号、答案、解析：12/12/12，答案序列与 DOCX 真值完全一致。
- 题图：仅题 4、8、11；解析图：仅题 2、6、8；题 8 同时显示两种角色的 2 张图。
- 内容审计：缺号、重号、缺答案、缺解析、LaTeX 语法错误、未渲染公式、键盘数学、JSON/占位符泄漏、错挂候选均为 0。
- 渲染：228 个 KaTeX 节点，0 渲染错误，0 原始 LaTeX 片段。
- 刷新持久化、题 4 入库、题库检索、选题、组卷和打印全部通过；打印 1 题、1 图、1 张 A4，0 坏图、0 溢出。

用户授权的 5 次完整双 PDF 现已全部使用：累计 35 次上游调用，每轮均受调用上限和模型白名单约束。

## UI 与基础设施验收

- 117 个点击动作全部进入双向动作清单，具备唯一 ID、所有者、风险等级、fixture、副作用和自动行为证据或隔离操作程序。
- 顶层导航、题库三种模式、搜索、五类筛选、重置、录题三标签、知识点面板、批量创建/返回、三种打印模式均在真实浏览器通过。
- 完整 DOCX/PDF 流程额外覆盖文件选择、用途确认、创建任务、审核、保存、提交、入库、检索、选题、组卷和打印。
- 静态 Tailwind CSS 冷启动无外网请求、无 production warning；页面错误和新增控制台错误为 0。
- 本机服务恶意 Origin 在上游调用前返回 403；生产依赖安全审计为 0 漏洞。

## 最终自动门禁

- `npm run verify:safe`：1,240 项测试，1,232 通过、0 失败、8 个真实材料/付费场景显式跳过；mock smoke 与 no-real-AI 守卫通过。
- 真实 DOCX general/layout：2 通过、0 失败（打印场景由完整双 DOCX 独立通过）。
- 简略与完整双 DOCX：各 1 通过、0 失败。
- 第 5 次完整双 PDF：1 通过、0 失败。
- 源材料结构审计：16/16 通过。

## 不能虚报为“绝对完美”的边界

本验收证明当前 16 个真实材料及冻结的通用 fixture 全部满足安全与内容契约。它不能数学上保证世界上任意未知、损坏或新格式文件永不失败；未知结构仍必须显式报错或进入人工审核，而不是猜测绑定。当前版本的正确承诺是：已覆盖格式完整通过，证据不足时安全失败，不污染题库。
